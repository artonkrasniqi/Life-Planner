/* Kennung der ausgelieferten Fassung.

   Steht sichtbar in der App (System → Über und unten in der Leiste),
   damit sich aus der Ferne feststellen lässt, ob ein Gerät wirklich
   die neue Fassung geladen hat oder noch eine alte aus dem Cache. */

export const BUILD = '2026-08-10 · 6';
